// String functions:

// length
// Description: Returns the length of a string
// Example: var str = "Hello, World!";
//          var len = str.length;

// indexOf()
// Description: Returns the index within the calling String object of the first occurrence of the specified value, or -1 if not found
// Example: var str = "Hello, World!";
//          var index = str.indexOf("o");

// lastIndexOf()
// Description: Returns the index within the calling String object of the last occurrence of the specified value, or -1 if not found
// Example: var str = "Hello, World!";
//          var index = str.lastIndexOf("o");

// charAt()
// Description: Returns the character at the specified index
// Example: var str = "Hello, World!";
//          var char = str.charAt(1);

// substring()
// Description: Returns a subset of a string between one index and another, or through the end of the string
// Example: var str = "Hello, World!";
//          var subStr = str.substring(0, 5);

// slice()
// Description: Returns a section of a string, optionally accepting negative indices to count from the end of the string
// Example: var str = "Hello, World!";
//          var subStr = str.slice(-6);

// replace()
// Description: Returns a new string with some or all matches of a pattern replaced by a replacement
// Example: var str = "Hello, World!";
//          var newStr = str.replace("Hello", "Hi");

// toLowerCase()
// Description: Returns a new string with all characters converted to lowercase
// Example: var str = "Hello, World!";
//          var lowerCaseStr = str.toLowerCase();

// toUpperCase()
// Description: Returns a new string with all characters converted to uppercase
// Example: var str = "Hello, World!";
//          var upperCaseStr = str.toUpperCase();

// trim()
// Description: Removes whitespace from both ends of a string and returns the modified string
// Example: var str = "   Hello, World!   ";
//          var trimmedStr = str.trim();

// split()
// Description: Splits a string into an array of substrings based on a specified separator and returns the array
// Example: var str = "apple,banana,orange";
//          var arr = str.split(",");

// concat()
// Description: Combines two or more strings into one new string
// Example: var str1 = "Hello, ";
//          var str2 = "World!";
//          var concatStr = str1.concat(str2);
