// Array functions:

// length
// Description: Returns the number of elements in an array
// Example: var arr = [1, 2, 3];
//          var len = arr.length;

// push()
// Description: Adds one or more elements to the end of an array and returns the new length of the array
// Example: var arr = [1, 2, 3];
//          arr.push(4, 5);

// pop()
// Description: Removes the last element from an array and returns that element
// Example: var arr = [1, 2, 3];
//          var lastElement = arr.pop();

// shift()
// Description: Removes the first element from an array and returns that element
// Example: var arr = [1, 2, 3];
//          var firstElement = arr.shift();

// unshift()
// Description: Adds one or more elements to the beginning of an array and returns the new length of the array
// Example: var arr = [1, 2, 3];
//          arr.unshift(-2, -1, 0);

// slice()
// Description: Returns a shallow copy of a portion of an array into a new array object selected from begin to end (end not included)
// Example: var arr = [1, 2, 3, 4, 5];
//          var newArr = arr.slice(2, 4);

// concat()
// Description: Returns a new array that is the result of concatenating two or more arrays
// Example: var arr1 = [1, 2, 3];
//          var arr2 = [4, 5, 6];
//          var newArr = arr1.concat(arr2);

// join()
// Description: Returns a string representing the elements of an array joined together with a specified separator
// Example: var arr = [1, 2, 3];
//          var str = arr.join('-');

// reverse()
// Description: Reverses the order of the elements in an array in place, and returns the modified array
// Example: var arr = [1, 2, 3];
//          arr.reverse();

// sort()
// Description: Sorts the elements of an array in place and returns the sorted array
// Example: var arr = [3, 1, 2];
//          arr.sort();

// splice()
// Description: Changes the contents of an array by removing or replacing existing elements and/or adding new elements
// Example: var arr = [1, 2, 3, 4];
//          arr.splice(2, 0, 2.5, 2.75);

